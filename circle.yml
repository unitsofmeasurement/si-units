general:
  artifacts:
    - "target" # files, relative to the build directory
machine:
  java:
    version: oraclejdk8
  environment:
    UOM_VERSION: 0.5-SNAPSHOT
test:
  post:
    # - cp $HOME/.m2/repository/si/uom/si-quantity/$UOM_VERSION/*.jar $CIRCLE_ARTIFACTS
    # - cp $HOME/.m2/repository/si/uom/si-impl/$UOM_VERSION/*.jar $CIRCLE_ARTIFACTS
    #- mvn deploy -Dmaven.test.skip -DcreateDocs=true -s settings.xml
